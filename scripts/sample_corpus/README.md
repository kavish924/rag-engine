Drop sample markdown / txt / html / pdf documentation files here.
`scripts/seed_corpus.py` will index everything in this folder on startup
so reviewers can test the system immediately without providing their own
corpus.
